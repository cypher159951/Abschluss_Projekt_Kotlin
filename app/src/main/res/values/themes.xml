<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PayMessage" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimaryDay</item>
        <item name="colorPrimaryDark">#47627C</item>
        <item name="android:textColor">@color/textColorPrimaryDay</item>
    </style>

    <style name="Theme.PayMessage" parent="Base.Theme.PayMessage" />
</resources>